import { generalStyles } from "../../../lib/styles";

export default async function Page() {
  return (
    <main>
      <h1 className={`${generalStyles.headText}`}>Onboarding</h1>
    </main>
  );
}
